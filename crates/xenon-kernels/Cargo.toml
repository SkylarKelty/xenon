[package]
name = "xenon-kernels"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Hand-rolled CUDA kernels (RMSNorm, GELU, RoPE, FMHA) for xenon."

[dependencies]
